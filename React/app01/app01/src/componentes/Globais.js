export default class Globais{
    static canal = 'Variável global'
}

// As variáveis globais só são usadas dentro de classes
// A alteração e redenrização, pode ser usada o state, para a manipulação dessas variáveis globais, colocando o valor de useState(Globais.variavel)